import React from 'react';
// import useSøknad from 'app/utils/hooks/useSøknad';
// import Api from 'app/api/api';

const FørstegangssøknadMedAnnenPart = () => {
    // !søknad.erEndringssøknad &&
    // eksisterendeSak !== undefined &&
    // eksisterendeSak.erAnnenPartsSak &&
    // eksisterendeSak.uttaksplan &&
    // eksisterendeSak.grunnlag &&
    // eksisterendeSak.uttaksplan.length > 0 &&
    // skalKunneViseMorsUttaksplanForFarEllerMedmor(eksisterendeSak.grunnlag, søknadsinfo)
    // const { eksisterendeSakData } = Api.useGetEksisterendeSak();

    // const { erEndringssøknad } = useSøknad();

    // const shouldRender = !erEndringssøknad && erFødsel;

    // if (!shouldRender) {
    //     return null;
    // }

    return <div>Førstegangsøknad med annen part Scenario9</div>;
};

export default FørstegangssøknadMedAnnenPart;
