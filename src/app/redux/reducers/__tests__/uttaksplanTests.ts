import søknadReducer from '../søknadReducer';
import actions from '../../actions/søknad/søknadActionCreators';
import { Periodetype } from '../../../types/uttaksplan/periodetyper';
// import { Periode } from '../../../types/uttaksplan/periodetyper';

// const perioder: Periode[] = [];

describe('Søknad - Uttaksplan reducer', () => {
    // it('should return initial state', () => {
    //     expect(søknadReducer(undefined, actions.uttaksplanAddPeriode({
    //         type: Periodetype
    //     }))).toEqual({});
    // });
});
