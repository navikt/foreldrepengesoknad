/// <reference types="vitest" />
import { createSharedPackagesConfig } from '@navikt/fp-config-vite';

// eslint-disable-next-line import-x/no-default-export
export default createSharedPackagesConfig(undefined);
