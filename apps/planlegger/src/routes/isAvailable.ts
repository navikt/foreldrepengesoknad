import PlanleggerRoutes from './routes';

const isAvailable = (route: PlanleggerRoutes): boolean => {
    switch (route) {
        default:
            return true;
    }
};

export default isAvailable;
