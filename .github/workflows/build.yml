name: Bygg og deploy
on:
    push:
        branches-ignore: # Kan legge til disse store feature branchene her
            - 'uttaksplanbuilder-rework'
        paths-ignore:
            - '**.md'
            - '**.MD'
            - '.gitignore'
            - '.editorconfig'
            - 'LICENCE'
            - 'CODEOWNERS'

jobs:
    build-app:
        name: Build
        uses: navikt/fp-gha-workflows/.github/workflows/build-app-frontend-pnpm.yml@turborepo
        with:
            build-image: true # default: true
            push-image: true # default: false
            legacy-peer-deps: false
        secrets: inherit

    promote-foreldrepengesoknad:
        name: Deploy til dev
        needs: build-app
        uses: navikt/fp-gha-workflows/.github/workflows/deploy-app-pnpm.yml@turborepo
        with:
            image: ghcr.io/${{ github.repository }}/foreldrepengesoknad:latest
            environment: dev
            platform: gcp
        secrets: inherit
