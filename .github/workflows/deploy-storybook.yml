name: 'Deploy storybook'

on:
  push:
    branches: ['master']
    paths-ignore:
      - '.github/workflows/build-redirect.yml'
      - 'server/**'
      - 'server-uinnlogget/**'
      - '.github/workflows/build-server.yml'

jobs:
  deploy-storybook:
    permissions:
      contents: write
      pages: write
    uses: navikt/fp-gha-workflows/.github/workflows/deploy-storybook.yml@e3d0b05ca3f5a9ece468aa89838fbd69188d0d96
    with:
      package-manager: pnpm
    secrets:
      READER_TOKEN: ${{ secrets.READER_TOKEN }}
